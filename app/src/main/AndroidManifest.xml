<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- CloudUploadWorker.kt runs as a real foreground service (setForeground,
         not setExpedited - expedited work's execution window is too short
         for a run that can take up to ~40 minutes) so the upload survives
         app backgrounding. FOREGROUND_SERVICE_DATA_SYNC is required
         specifically for the dataSync foreground service type on API 34+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Progress/completion notifications for the cloud upload (API 33+
         requires this to be requested at runtime too - see MainActivity.kt). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.ForgeScanMobile">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="subject_segment" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager's own merged manifest declares this service with no
             foregroundServiceType, which on API 34+ makes CloudUploadWorker.kt's
             setForeground(..., FOREGROUND_SERVICE_TYPE_DATA_SYNC) call crash at
             runtime ("foregroundServiceType 0x1 is not a subset of ... 0x0 in
             service element of manifest file") - confirmed on a real device.
             Overriding the type here is the documented fix. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
